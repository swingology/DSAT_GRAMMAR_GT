---
description: graft ask — locate + understand code via the graft graph
argument-hint: <question>
allowed-tools: Bash(graft ask:*)
---

Run `graft ask` for the user's question and answer using its output.

!`graft ask "$ARGUMENTS" --source`

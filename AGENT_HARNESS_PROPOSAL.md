Let me look at the project to understand what the questions generator is and how it currently works.[27;5;106~[27;5;106~Let me read the key sections of the generation tasks file to understand the planned flow and existing foundation.[27;5;106~[27;5;106~Now let me look at the existing generation router to see what's already built.[27;5;106~[27;5;106~Let me look at the existing generation pipeline code to see the current shape.[27;5;106~[27;5;106~Good question. Your generation system is already most of a harness — it's just spread across FastAPI routers, LLM providers, job runners, and review logic rather than packaged as one coherent "agent runtime." Let me map the harness concept onto what you've actually built and what a unified harness would look like.[27;5;106~[27;5;106~## What you already have (the pieces of a harness)[27;5;106~[27;5;106~Your `TASKS_GENERATION.md` Phases 0–10 describe a multi-stage pipeline. Each stage corresponds to a harness component:[27;5;106~[27;5;106~| Harness component | What you have today | Where it lives |[27;5;106~|---|---|---|[27;5;106~| **Tool runtime** | LLM generation call, JSON parser, validator, overlap detector, YAML exporter | `backend/app/routers/generate.py`, `app/parsers/`, `app/pipeline/` |[27;5;106~| **Sandbox / permissions** | Admin-required auth, `practice_status=draft` gate, `admin_review_required` release policy | `app/auth.py`, `generate.py` |[27;5;106~| **Context management** | Rules markdown loaded into prompt (`rules_agent_..._v8.md` + `rules_agent_dsat_reading_v2.md`), source official examples injected | `app/prompts/generate_prompt.py` |[27;5;106~| **Sub-agent / delegation** | Review swarm: OpenAI + Claude + DeepSeek run concurrently against the same candidate | `app/review/runner.py` with semaphore |[27;5;106~| **Skills / capabilities** | The v8 rules file is essentially a "skill" — 6000+ lines of generation rubric, distractor heuristics, taxonomy | `rules_agent_dsat_grammar_ingestion_generation_v8.md` |[27;5;106~| **Loop control** | Per-job retry (`GENERATION_JOB_MAX_RETRIES=3`), transient vs permanent failure classification, batch counter finalization | `generate.py` (`_is_transient_error`, `_run_batch_job`) |[27;5;106~| **Telemetry / memory** | `GenerationBatch`, `QuestionJob`, `ReviewRun`, `LlmReviewResult`, `ConsensusVerdict`, `AutoReleaseAuditLog` | `app/models/db.py` |[27;5;106~[27;5;106~So the harness already exists — it's just **distributed across backend modules** rather than wrapped in a single agent runtime. The generation "agent" is really a pipeline, not a reasoning loop. That's an important distinction.[27;5;106~[27;5;106~## Pipeline vs. agent harness[27;5;106~[27;5;106~Your current system is a **fixed pipeline**: generate → parse → validate → persist → overlap → review → consensus → admin. Each step runs once per question. There's no reasoning loop where the generator looks at its own output, decides it's bad, and tries again with a different strategy.[27;5;106~[27;5;106~A true **agent harness** for the question generator would add a reasoning loop on top:[27;5;106~[27;5;106~```mermaid[27;5;106~graph TD[27;5;106~    A[Generation Request] --> B[Agent picks strategy: grammar focus, trap, difficulty][27;5;106~    B --> C[LLM generates candidate][27;5;106~    C --> D{Validation checklist pass?}[27;5;106~    D -->|No| E[Agent diagnoses failure][27;5;106~    E --> B[27;5;106~    D -->|Yes| F[Persist as draft][27;5;106~    F --> G[Overlap detection][27;5;106~    G --> H{Overlap found?}[27;5;106~    H -->|Yes| I[Agent rewrites passage/stem][27;5;106~    I --> C[27;5;106~    H -->|No| J[Review swarm scores candidate][27;5;106~    J --> K{Consensus verdict}[27;5;106~    K -->|reject| L[Agent reads scores + reasons][27;5;106~    L --> B[27;5;106~    K -->|needs human| M[Admin dashboard queue][27;5;106~    K -->|accept| M[27;5;106~```[27;5;106~[27;5;106~The key difference: the agent **reads its own failure signals and decides what to change**, rather than just retrying the same prompt or failing to a terminal status.[27;5;106~[27;5;106~## What a concrete harness would look like[27;5;106~[27;5;106~If you wrapped your generator in an explicit agent harness, here's what each piece would be:[27;5;106~[27;5;106~### 1. Tool palette (what the agent can call)[27;5;106~[27;5;106~```python[27;5;106~tools = [[27;5;106~    load_official_examples,      # already exists: _load_official_source_examples[27;5;106~    generate_question,           # already exists: LLM call via app/llm/[27;5;106~    parse_candidate_json,        # already exists: extract_json_from_text[27;5;106~    validate_candidate,          # already exists: validate_question[27;5;106~    check_overlap,               # already exists: detect_overlaps[27;5;106~    run_review_swarm,            # already exists: app/review/runner.py[27;5;106~    get_consensus_verdict,       # already exists: app/review/consensus.py[27;5;106~    save_candidate,              # already exists: persist Question/Version/Annotation/Options[27;5;106~    read_rubric_section,         # NEW: let agent look up specific rules on demand[27;5;106~    query_past_failures,         # NEW: "last 5 questions with this focus key failed how?"[27;5;106~][27;5;106~```[27;5;106~[27;5;106~Most of these already exist as functions in your backend. The harness would expose them as tools the agent calls in a loop rather than as sequential pipeline steps.[27;5;106~[27;5;106~### 2. System prompt / skill loading[27;5;106~[27;5;106~Instead of dumping the entire 6000-line v8 rules file into every generation call, the harness would load it as a **skill** the agent can search:[27;5;106~[27;5;106~```python[27;5;106~system_prompt = """[27;5;106~You are a DSAT question generation agent.[27;5;106~You have access to the grammar generation rules (v8) and reading rules (v2).[27;5;106~Before generating, load the passage construction rules for your target grammar_focus_key.[27;5;106~After generating, run the validation checklist. If it fails, diagnose and regenerate.[27;5;106~"""[27;5;106~```[27;5;106~[27;5;106~This is the same pattern as the Zed agent loading the `tdd` or `diagnose` skill — the rules become a reference the agent consults, not a monolithic blob in the prompt.[27;5;106~[27;5;106~### 3. Reasoning loop (the missing piece)[27;5;106~[27;5;106~Your current pipeline does one generation attempt per job. A harness would let the agent iterate:[27;5;106~[27;5;106~```python[27;5;106~async def generation_loop(request: GenerationRequest) -> Question:[27;5;106~    for attempt in range(MAX_ATTEMPTS):[27;5;106~        strategy = agent.plan(request, past_failures=failures)[27;5;106~        candidate = agent.generate(strategy, source_examples)[27;5;106~        [27;5;106~        validation = validate_candidate(candidate)[27;5;106~        if not validation.ok:[27;5;106~            failures.append({"attempt": attempt, "error": validation.error})[27;5;106~            continue  # agent re-plans with the failure signal[27;5;106~        [27;5;106~        question = save_candidate(candidate)[27;5;106~        overlap = check_overlap(question)[27;5;106~        if overlap.unresolved:[27;5;106~            failures.append({"attempt": attempt, "overlap": overlap})[27;5;106~            continue  # agent rewrites to avoid overlap[27;5;106~        [27;5;106~        review = run_review_swarm(question)[27;5;106~        verdict = get_consensus_verdict(review)[27;5;106~        [27;5;106~        if verdict == "reject":[27;5;106~            failures.append({"attempt": attempt, "scores": review.scores})[27;5;106~            continue  # agent reads the low scores and adjusts[27;5;106~        [27;5;106~        return question  # needs_human_review or accept → hand to admin[27;5;106~    [27;5;106~    return question  # best attempt, even if not perfect[27;5;106~```[27;5;106~[27;5;106~### 4. Sandbox / safety (you already have this right)[27;5;106~[27;5;106~Your design decisions are already harness-grade safety:[27;5;106~- Generated questions are `draft` until admin approves — the agent can't self-publish[27;5;106~- Self-study agent requests are forced to `admin_review_required` — can't bypass review[27;5;106~- Auto-release (Phase 10) has 8 gates + kill switch — the agent can't release on its own[27;5;106~- Overlap detection blocks approval — the agent can't accidentally clone official questions[27;5;106~[27;5;106~### 5. Memory / provenance[27;5;106~[27;5;106~You already track this well:[27;5;106~- `QuestionJob.generation_request_jsonb` — what was asked[27;5;106~- `generation_provenance` — what template/chain was used[27;5;106~- `ReviewRun` + `LlmReviewResult` — what reviewers said[27;5;106~- `AutoReleaseAuditLog` — why a question was auto-released[27;5;106~[27;5;106~A harness would add **cross-batch memory**: "the last 20 questions with `grammar_focus_key=pronoun_antecedent_agreement` had low `distractor_quality_score` — adjust distractor strategy." This is the piece you don't have yet — the generator doesn't learn from past batches.[27;5;106~[27;5;106~## The honest assessment[27;5;106~[27;5;106~Your system is a **well-engineered pipeline with review gates**, not an agent harness. The difference matters:[27;5;106~[27;5;106~- **Pipeline** (what you have): fixed steps, one attempt per question, retry only on transient errors, human is the adaptive intelligence.[27;5;106~- **Agent harness** (what you could add): the generator itself reads failure signals, adjusts strategy, and iterates until quality clears — with humans still as the final gate.[27;5;106~[27;5;106~The building blocks are all there. The missing piece is a reasoning loop that connects `validate_candidate` failures, `overlap` hits, and `review_swarm` low scores back into a re-planning step before the next generation attempt — instead of just marking the job `failed_permanent` and moving on.[27;5;106~[27;5;106~Whether that's worth building depends on your failure rate. If most generations pass validation and review on the first try, the pipeline is fine. If you're seeing a lot of `failed_permanent` jobs or `reject` verdicts that a human has to manually diagnose and re-request, then the adaptive loop would pay for itself.

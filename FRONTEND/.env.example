VITE_API_BASE_URL=http://localhost:8000
VITE_STUDENT_API_KEY=student-test-key

# Dev test user (Phase 1-2). Seed: POST /users with admin key, body {"username":"test-student"}.
# Remove these two vars in Phase 3 when Supabase auth is wired up.
VITE_TEST_USER_TOKEN=<uuid from POST /users response>
VITE_TEST_USER_ID=<integer id from POST /users response>

# Phase 3 only — add when auth workstream begins:
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
